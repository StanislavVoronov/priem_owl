import { styled } from '@material-ui/core/styles';
import { ExpansionPanelDetails } from '$components';

export const PanelDetails = styled(ExpansionPanelDetails)(() => {
	return {
		margin: '0 0 36px 0',
		padding: 0,
	};
});
